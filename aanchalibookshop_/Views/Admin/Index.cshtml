@model aanchalibookshop_.userdetail

@{ 
    ViewBag.Title = "Index";
}

<h2>Welcome to Admin login </h2>
<h2> This is index page of Admin Controller</h2>
<table>
    @{ 
        Html.BeginForm("Index", "Admin", FormMethod.Post);
        <tr>
            <td>User Name:</td>
            <td>@Html.TextBoxFor(a  => a.u_name)  </td>
        </tr>
        <tr>
            <td>Password:</td>
            <td>@Html.PasswordFor(a  => a.u_password)  </td>
        </tr>
        <tr> 
            <td colspan="2"><input type="submit" value="login" /></td>
        </tr> 
        <td colspan="2">@ViewBag.msg</td>
    }
</table>

